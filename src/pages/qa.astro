---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Q&A · AI Daily Insights" description="AI Daily Insights Q&A and agent usage guide">
  <a class="backnav" href="/">
    <span class="back-arrow">←</span>
    <span class="back-label">返回 AI Daily 首页</span>
    <span class="back-cmd">cd ~/news</span>
  </a>
  <div class="cmd"><span class="pr">$</span>cat ./qa.md <span class="flag">--for=humans,agents</span></div>

  <article class="md qa">
    <h1>❯ AI Daily Insights 是什么？</h1>
    <p><strong>AI Daily Insights</strong> 是一个面向人和 AI Agent 的每日 AI 资讯站。它不只把新闻写成人能快速扫读的文章，也把同一批内容整理成机器可发现、可引用、可检索的入口。</p>
    <p>核心目标很简单：人打开网页时，看到判断、脉络和信号；Agent 调用时，能拿到稳定 URL、结构化索引和明确的引用边界。</p>

    <h2>❯ 三种接入方式，先选一条</h2>
    <table>
      <thead>
        <tr><th>路线</th><th>适合谁</th><th>怎么开始</th></tr>
      </thead>
      <tbody>
        <tr><td><strong>MCP</strong>（推荐）</td><td>Claude / Cursor 等 AI 助手</td><td>一条命令注册，让 AI 替你读（见下文）</td></tr>
        <tr><td><strong>CLI</strong></td><td>习惯命令行</td><td><code>npx ai-daily-insights latest</code></td></tr>
        <tr><td><strong>JSON / RSS</strong></td><td>开发者 / 自建 Agent</td><td><code>GET /index.json</code>、<code>/feed.xml</code></td></tr>
      </tbody>
    </table>

    <h2>❯ 接入之后，可以怎么用？</h2>
    <p>接上 MCP 之后，你<strong>不用记任何命令</strong>，直接用大白话跟 AI 助手说话就行。它会自己判断该调哪个工具、去取哪天的数据。常见两类用法：</p>

    <div class="ask-block">
      <div class="ask-k">📰 日常速览</div>
      <ul class="ask-list">
        <li>看看今天的 AI Daily</li>
        <li>最近三天 AI 圈有什么大新闻？</li>
        <li>把这周的 AI Daily 总结成 5 条划重点</li>
      </ul>
    </div>

    <div class="ask-block">
      <div class="ask-k">🔍 公司 / 主题定向</div>
      <ul class="ask-list">
        <li>搜一下最近关于 Anthropic 的新闻</li>
        <li>OpenAI 这周有什么发布？讲讲细节</li>
        <li>帮我盯着英伟达和 HBM 内存相关的动态</li>
      </ul>
    </div>

    <p>这些大白话，背后由 AI 自动映射到对应工具，你无需关心：</p>
    <table>
      <thead>
        <tr><th>你想做什么</th><th>这样问它</th><th>背后调用</th></tr>
      </thead>
      <tbody>
        <tr><td>看最近几期</td><td>「最近有什么 AI 新闻」</td><td><code>list_latest</code></td></tr>
        <tr><td>看今天 / 最新一期</td><td>「看看今天的 AI Daily」</td><td><code>get_latest</code></td></tr>
        <tr><td>看某一天全部内容</td><td>「打开 6 月 24 日那期」</td><td><code>get_article</code></td></tr>
        <tr><td>看一段时间 / 这一周</td><td>「最近三天有什么大新闻」</td><td><code>get_range</code></td></tr>
        <tr><td>按标签列出某主题</td><td>「列出所有带 OpenAI 标签的期」</td><td><code>list_by_tag</code></td></tr>
        <tr><td>全文 / 标签检索</td><td>「搜一下关于 Anthropic 的新闻」</td><td><code>search</code></td></tr>
      </tbody>
    </table>

    <h2>❯ 为什么叫“为人与 Agent 而生”？</h2>
    <p>传统媒体页面默认给人读，API 默认给程序读。这个站点希望中间层更薄：同一篇内容同时有 HTML、RSS、JSON index 和 llms.txt，方便浏览器、订阅器、搜索引擎和 Agent 从不同入口进入。</p>

    <h2>❯ AI Agent 应该如何调用？</h2>
    <p>三步即可：先读 <code>/llms.txt</code> 理解站点结构，再读 <code>/index.json</code> 拿到文章列表与摘要，最后按需打开单篇文章 URL 取正文。所有端点都是公开的 <code>GET</code>，无需鉴权、无需 API Key。</p>

    <pre><code># 1. 发现：站点说明 + 端点清单
GET https://www.aidailyinsights.cn/llms.txt

# 2. 索引：拿到所有文章的日期 / 标题 / 摘要 / URL
GET https://www.aidailyinsights.cn/index.json

# 3. 取正文：打开某一天的文章页（HTML）
GET https://www.aidailyinsights.cn/2026-06-24/</code></pre>

    <p>如果你在用 Claude、ChatGPT 等带工具的助手，可以直接把下面这句话丢给它，它就会自己走完上面三步：</p>

    <pre><code>读取 https://www.aidailyinsights.cn/index.json，
列出最近 5 天的 AI 资讯标题和摘要；
我点哪条，你再打开对应的文章 URL 给我讲细节。</code></pre>

    <p>用 <code>curl</code> 命令行验证也可以：</p>

    <pre><code>curl -s https://www.aidailyinsights.cn/index.json | jq '.posts[0]'
curl -s https://www.aidailyinsights.cn/feed.xml      # RSS 订阅 / 轮询
curl -s 'https://www.aidailyinsights.cn/search?q=Anthropic&limit=3' | jq '.results[].title'
curl -s 'https://www.aidailyinsights.cn/search?tag=OpenAI' | jq '.count'   # 按标签筛</code></pre>

    <h2>❯ 用 MCP 一键接入（Claude / Cursor）</h2>
    <p>我们提供了官方 <strong>MCP server</strong>：<code>ai-daily-insights-mcp</code>，已发布到 npm。装上后，你的 AI 助手就能直接列出、读取、检索每日资讯，无需手动喂 URL。</p>
    <p><strong>Claude Code</strong> 一条命令即可：</p>
    <pre><code>claude mcp add ai-daily-insights -- npx -y ai-daily-insights-mcp</code></pre>
    <p><strong>Claude Desktop / Cursor</strong> 写进配置文件：</p>
    <pre><code>{`{
  "mcpServers": {
    "ai-daily-insights": {
      "command": "npx",
      "args": ["-y", "ai-daily-insights-mcp"]
    }
  }
}`}</code></pre>
    <p>提供六个工具：</p>
    <table>
      <thead>
        <tr><th>工具</th><th>作用</th></tr>
      </thead>
      <tbody>
        <tr><td><code>get_latest</code></td><td>最新一期，含全部新闻条目，一次取回。</td></tr>
        <tr><td><code>list_latest</code></td><td>最近 N 期的列表（日期 / 标题 / 摘要 / 标签 / URL）。</td></tr>
        <tr><td><code>get_article</code></td><td>按日期取单期结构化正文 <code>{`{index, title, signal, body}`}</code>。</td></tr>
        <tr><td><code>get_range</code></td><td>取某个日期区间内的所有期（如最近一周），并行抓取。</td></tr>
        <tr><td><code>list_by_tag</code></td><td>列出带某个标签（公司 / 主题）的期，标签不区分大小写。</td></tr>
        <tr><td><code>search</code></td><td>跨全站的全文 + 标签检索，由服务端 <code>/search</code> 一次返回。</td></tr>
      </tbody>
    </table>
    <p>响应会在本地做几分钟的内存缓存（默认 5 分钟，可用 <code>AI_DAILY_CACHE_TTL_MS</code> 调整），连续提问不会反复打站。源码与文档见 <a href="https://www.npmjs.com/package/ai-daily-insights-mcp">npm</a> / <a href="https://github.com/WhaleCupl/ai-daily-insights-mcp">GitHub</a>。</p>

    <h2>❯ 在终端里读（CLI）</h2>
    <p>如果你更习惯命令行，用 <code>ai-daily-insights</code> 这个 CLI 直接在终端速览，无需打开浏览器：</p>
    <pre><code>npx ai-daily-insights latest          # 最近 5 期
npx ai-daily-insights show 2026-06-24 # 某期全部新闻 + signal
npx ai-daily-insights search Anthropic # 跨期检索</code></pre>
    <p>和 MCP 共用同一套公开 JSON 接口，无需登录。源码见 <a href="https://www.npmjs.com/package/ai-daily-insights">npm</a> / <a href="https://github.com/WhaleCupl/ai-daily-insights-cli">GitHub</a>。</p>

    <h2>❯ Agent 调用协议</h2>
    <ul>
      <li><strong>发现入口：</strong><code>/llms.txt</code>，给 Agent 的站点说明和主要端点。</li>
      <li><strong>索引入口：</strong><code>/index.json</code>，包含日期、标题、摘要和文章 URL。</li>
      <li><strong>单期结构化：</strong><code>/{`{date}`}.json</code>，单期拆成 <code>{`{index, title, signal, body}`}</code> 条目。</li>
      <li><strong>检索入口：</strong><code>/search?q=…&amp;tag=…</code>，全文 + 标签检索，一次返回；<code>/search-index.json</code> 是全部条目的扁平索引。</li>
      <li><strong>订阅入口：</strong><code>/feed.xml</code>，适合自动更新和轮询。</li>
      <li><strong>引用规则：</strong>引用时保留文章 URL、发布日期和标题，不把摘要当成完整事实来源。</li>
    </ul>

    <blockquote>
      <p><strong>signal</strong> 对 Agent 来说，这个站点不是“网页外观优先”的内容库，而是“可被发现、可被引用、可被组合”的资讯节点。</p>
    </blockquote>

    <h2>❯ 最近增强了什么？</h2>
    <ul>
      <li><strong>结构化单期 JSON：</strong>每一期都有 <code>/{`{date}`}.json</code>，正文已拆成 <code>{`{index, title, signal, body}`}</code> 条目，Agent 无需解析 HTML。</li>
      <li><strong>全文 + 标签检索：</strong>新增服务端 <code>/search</code> 接口和 <code>/search-index.json</code> 扁平索引，一次调用即可跨全站检索。</li>
      <li><strong>MCP 扩到六个工具：</strong>新增 <code>get_range</code>（按时间段）、<code>list_by_tag</code>（按标签），并加了内存缓存与并行抓取。</li>
    </ul>
    <p>后续方向：来源链接与更新时间字段化、按主题聚合的专题页，以及更细粒度的条目级标签。</p>
  </article>
</Layout>
